import { SafeAreaView } from "react-native";
import Calculator from "./Calculator";


const App = () =>{
  
  return(
    <SafeAreaView style={{flex: 1}}>
      <Calculator/>
    </SafeAreaView>
  )
}

export default App